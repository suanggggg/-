# Frontend (React + Vite + AntD)

快速运行（需要 Node.js >= 18 推荐）:

```powershell
cd d:/code/软件体系结构/frontend
npm install
npm run dev
```

- 启动后打开 `http://localhost:5173`（Vite 默认端口）。
- 本地前端直接调用后端 `http://127.0.0.1:8000`，请确保后端服务已启动。
